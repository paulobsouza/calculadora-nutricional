# Variáveis para Docker Compose - Copie para .env e ajuste os valores

# MongoDB
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=sua_senha_mongo_segura

# JWT - IMPORTANTE: Gere uma chave segura
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=sua_chave_jwt_super_segura_aqui

# Frontend
VITE_API_URL=http://seu-dominio.com/api
VITE_USDA_API_KEY=sua_chave_usda
